query getPostCategories($where: RootQueryToCategoryConnectionWhereArgs) {
  categories(where: $where) {
    nodes {
      parentId
      slug
      name
      databaseId
      parentDatabaseId
      count
      uri
    }
  }
}
